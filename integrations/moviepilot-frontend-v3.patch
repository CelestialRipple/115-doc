--- a/src/components/cards/TorrentCard.vue
+++ b/src/components/cards/TorrentCard.vue
@@ -1,4 +1,5 @@
 <script lang="ts" setup>
+import { openPluginBrowserDownload } from '@/utils/moviepilotBrowserDownload'
 import type { PropType } from 'vue'
 import { formatFileSize, formatDateDifference } from '@/@core/utils/formatters'
 import api from '@/api'
@@ -75,6 +76,7 @@
   if (item && !isNullOrEmptyObject(item)) {
     downloadItem.value = item
   }
+  if (openPluginBrowserDownload(downloadItem.value?.torrent_info)) return
   // 打开下载对话框
   openSharedDialog(
     AddDownloadDialog,
--- a/src/components/cards/TorrentItem.vue
+++ b/src/components/cards/TorrentItem.vue
@@ -1,4 +1,5 @@
 <script lang="ts" setup>
+import { openPluginBrowserDownload } from '@/utils/moviepilotBrowserDownload'
 import type { PropType } from 'vue'
 import { formatFileSize, formatDateDifference } from '@/@core/utils/formatters'
 import api from '@/api'
@@ -71,6 +72,7 @@
 
 // 询问并添加下载
 async function handleAddDownload() {
+  if (openPluginBrowserDownload(torrent.value)) return
   // 打开下载对话框
   openSharedDialog(
     AddDownloadDialog,
--- a/src/components/dialog/SiteResourceDialog.vue
+++ b/src/components/dialog/SiteResourceDialog.vue
@@ -1,4 +1,5 @@
 <script setup lang="ts">
+import { openPluginBrowserDownload } from '@/utils/moviepilotBrowserDownload'
 import api from '@/api'
 import type { Site, TorrentInfo, SiteCategory } from '@/api/types'
 import { formatFileSize } from '@core/utils/formatters'
@@ -137,6 +138,7 @@
 
 // 添加下载
 async function addDownload(_torrent: TorrentInfo) {
+  if (openPluginBrowserDownload(_torrent)) return
   torrent.value = _torrent
   addDownloadDialog.value = true
 }
--- /dev/null
+++ b/src/utils/moviepilotBrowserDownload.ts
@@ -0,0 +1,23 @@
+/** 只接受本插件、本资源、同源的短时浏览器下载地址。 */
+export function openPluginBrowserDownload(torrent?: {
+  site_name?: string; enclosure?: string; page_url?: string;
+}): boolean {
+  try {
+    if (torrent?.site_name !== '腾讯文档115媒体库' || !torrent.enclosure || !torrent.page_url) return false
+    const marker = new URL(torrent.enclosure)
+    const resourceId = marker.searchParams.get('x.td115')
+    if (marker.protocol !== 'magnet:' || !resourceId) return false
+    const detail = new URL(torrent.page_url, location.origin)
+    if (detail.origin !== location.origin || !detail.hash.startsWith('#mp115-browser=')) return false
+    const value = new URLSearchParams(detail.hash.slice('#mp115-browser='.length)).get('url')
+    if (!value) return false
+    const target = new URL(value, location.origin)
+    const token = target.searchParams.get('token') || ''
+    if (target.origin !== location.origin
+      || target.pathname !== '/api/v1/plugin/TencentDoc115Library/resources/browser/' + encodeURIComponent(resourceId)
+      || !/^\d{1,12}\.[a-f0-9]{64}$/.test(token)
+      || Number(token.split('.')[0]) * 1000 <= Date.now()) return false
+    window.open(target.href, '_blank', 'noopener,noreferrer')
+    return true
+  } catch { return false }
+}
